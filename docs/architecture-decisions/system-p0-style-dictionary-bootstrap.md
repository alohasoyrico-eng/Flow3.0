# System P0 Style Dictionary bootstrap

Generated: 2026-08-11

This report records the first concrete remediation step for P0.1. It is not the end of foundations remediation.

## What changed

- `style-dictionary` is now a real development dependency.
- `style-dictionary.config.mjs` exists.
- `packages/tokens/source/flow.tokens.json` exists as the initial DTCG-style token source.
- `npm run build:tokens` now runs Style Dictionary.
- Style Dictionary generates:
  - `packages/tokens/styles/tokens.css`
  - `packages/tokens/tokens.json`
  - `packages/tokens/src/generated/tokens.ts`

## Current output

- Token contract format: `flow-token-contract@2`
- Token source: `packages/tokens/source/**/*.tokens.json`
- Generated by: `style-dictionary`
- Token count: `1078`
- CSS output has `0` `undefined` values after verification.

## Gate movement

`audit:forensic-gates` now reports:

- `style-dictionary-real`: `pass`
- `typescript-source-real`: `fail`, because FlowDocs still has zero `.ts/.tsx` source files.
- `primitive-cascade-runtime`: `fail`, because primitives still lack runtime/TS coverage.
- `docs-ownership`: `fail`, because docs still contain hand-authored system surfaces.

## Important limitation

The first source file was bootstrapped from the legacy CSS so we could invert the build direction safely. That means P0.1 is only partially complete:

- The pipeline is real.
- The source still needs curation into foundation/primitive families.
- CSS is now generated output, but legacy naming and docs-specific tokens still exist in the source.
- Foundation ownership still needs explicit taxonomy.

## Next required step

Continue P0.1 by splitting `flow.tokens.json` into curated foundation/primitive source files and marking docs-only/system-only token families before primitive runtime work begins.
