{
  "components": {
    "copy-button": {
      "api-foundations": {
        "copy": "Copy Button exposes one explicit value, accessible naming, feedback timing, state, density, and clipboard callbacks through package props.",
        "props": [
          {
            "name": "value",
            "type": "string",
            "required": "yes",
            "notes": "Exact value written to the clipboard."
          },
          {
            "name": "label",
            "type": "string",
            "required": "conditional",
            "notes": "Visible text for text and inline variants."
          },
          {
            "name": "ariaLabel",
            "type": "string",
            "required": "conditional",
            "notes": "Required when the visible label is absent or ambiguous."
          }
        ]
      }
    }
  }
}
