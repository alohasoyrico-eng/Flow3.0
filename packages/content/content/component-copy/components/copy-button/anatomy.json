{
  "components": {
    "copy-button": {
      "anatomy": {
        "items": [
          {
            "part": "button root",
            "rule": "Uses native Button or Icon Button semantics.",
            "tokens": ["comp.copy-button.*", "comp.button.*", "comp.icon-button.*"]
          },
          {
            "part": "label or icon",
            "rule": "Names the copied target through visible text or an accessible name.",
            "tokens": ["sys.voice.*", "sys.iconography.*"]
          },
          {
            "part": "feedback",
            "rule": "Exposes copied and error states with recovery language.",
            "tokens": ["sys.state.*", "sys.tone.*"]
          }
        ]
      }
    }
  }
}
